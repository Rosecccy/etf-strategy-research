# Live optimizer ledgers

Runtime ledgers are append-only CSV files. Existing keyed rows must never be rewritten. The repository keeps only schemas/documentation; live evidence should be managed as runtime artifacts unless explicitly approved for publication.
