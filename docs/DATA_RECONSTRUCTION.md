# Data Reconstruction

当前 GitHub 仓库故意不继续新增大体积原始行情、Parquet 特征面板、Joblib 模型和重复研究中间表。完整 2026-08-21 审计包仍是这些归档文件的来源。

## GitHub 中保留

- 历史 `strategies/` 代码与可运行底座；
- 当前 `same_day_1445/release_v2/` 正式结果；
- 关键审计、重建与发布脚本；
- 后续优化协议和冻结基线。

## 全量复跑时恢复

需要严格全量重建时，从原始审计包恢复 C/S/D/R 的原始行情、特征面板和模型文件，再执行对应脚本。不要把无法重建的大文件静默替换成新版本。

## 原则

GitHub 管代码、配置、正式小型账本与审计证据；大数据和模型二进制作为可恢复研究工件管理。这样后续每次策略升级的 diff 都能直接审查。
