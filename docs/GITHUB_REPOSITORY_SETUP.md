# GitHub Repository Setup

当前仓库 `Rosecccy/etf-strategy-research` 为 Public。公开仓库只提交适合公开共享的研究代码、冻结结果、说明文档和经过检查的数据文件。

## 公开仓库要求

- 当前正式结果以 `same_day_1445/release_v2` 为准；
- 本地运行日志和机器相关临时文件不进入正式版本；
- 大型研究工件以可恢复方式单独管理，不为了“完整”重复塞入 Git；
- 需要受控共享的材料使用独立的受控存储，不改变当前公开仓库对正式基线的定义。
